// ─────────────────────────────────────────────────────────
// 향후 API / 비즈니스 로직을 이 디렉토리(src/)에 작성하세요.
//
// 예시 구조:
//   src/
//   ├── routes/
//   │   └── api.ts        ← REST API 라우트
//   ├── services/
//   │   └── verify.ts     ← 검증 비즈니스 로직
//   ├── models/
//   │   └── project.ts    ← DB 모델 (TypeORM / Prisma 등)
//   └── index.ts          ← 이 파일
//
// 빌드: npm run build  →  dist/ 에 JS 산출물 생성
// ─────────────────────────────────────────────────────────

export { };
console.log('[src] TypeScript module loaded (placeholder)');
