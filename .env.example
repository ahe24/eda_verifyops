# ─────────────────────────────────────────────────────────
# VerifyOps — 환경변수 설정 템플릿
#
# 사용법:
#   cp .env.example .env
#   vi .env          ← 실제 값으로 수정
#
# ⚠️  .env 는 절대 git 에 커밋하지 마세요 (.gitignore 에 포함됨)
# ─────────────────────────────────────────────────────────

# ── 서버 설정 ──────────────────────────────────────────────
# 바인딩 주소 (0.0.0.0 = 모든 인터페이스, 127.0.0.1 = 로컬만)
HOST=0.0.0.0

# 리스닝 포트 (nginx 리버스 프록시 사용 시 외부에 직접 열지 않아도 됨)
PORT=3000

# ── Node 환경 ──────────────────────────────────────────────
NODE_ENV=production

# ── PM2 앱 이름 (pm2 list 에서 표시되는 이름) ──────────────
APP_NAME=verifyops-promo

# ── 향후 확장 예시 (필요 시 주석 해제) ────────────────────
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=verifyops
# DB_USER=verifyops_user
# DB_PASS=your_secure_password
#
# JWT_SECRET=your_jwt_secret
# SESSION_SECRET=your_session_secret
